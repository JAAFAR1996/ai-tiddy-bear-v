#include "config.h"

// WiFi configuration (managed by WiFiManager)
const char* WIFI_SSID = "";
const char* WIFI_PASSWORD = "";

// Server configuration (initialized with defaults)
const char* SERVER_HOST = DEFAULT_SERVER_HOST;
const int SERVER_PORT = DEFAULT_SERVER_PORT;
const char* WEBSOCKET_PATH = DEFAULT_WEBSOCKET_PATH;